the program should execute as specified in the assignment breif, it should be executed from the CLI without problem, the working directory does not need
to be specifed as it automatically gets the working directory with `pwd`

note that the microphone (because the labs did not have a microphone) uses the default microphone found by "pulse"
form ffmpeg
